/**
 * Common identifier aliases used across the domain.
 *
 * These are intentionally plain string aliases. Introducing branded/nominal
 * types would add ceremony without a current need, so we keep them structural
 * and document the intent here.
 */
export type PlayerId = string;

export type RaceTimeUserId = string;

export type SpeedrunComUserId = string;
